# V1.4 改进计划

## 连连看游戏（Android）

| 项目 | 内容 |
|---|---|
| 文档版本 | V1.0 |
| 编制日期 | 2026-09-12 |
| 基线版本 | V1.3（`versionCode = 4`） |
| 依据 | 开发者提出的两项需求：① 给 APP 绘制图标；② 解决启动瞬间白屏 |
| 计划交付 | `versionCode = 5`、`versionName = "1.4"` |
| 状态 | **已实施**（2026-09-12）—— 两项需求均已落地，构建与 194 个 JVM 单测全绿；结果见 §10，真机验收项见 §10.6 |

---

## 1. 现状核查

### 1.1 图标（需求 ①）

| 项 | 现状 |
|---|---|
| 自适应图标（API 26+） | `mipmap-anydpi-v26/ic_launcher.xml` 与 `ic_launcher_round.xml`，引用 `drawable/ic_launcher_foreground.xml`(1.7KB) 与 `ic_launcher_background.xml`(5.6KB) |
| 光栅图标（API 24–25） | `mipmap-{m,h,xh,xxh,xxxh}dpi/ic_launcher.webp` 与 `ic_launcher_round.webp`，共 10 个文件 |
| 内容 | **全部是 Android Studio 模板素材**（绿色机器人那套），与本游戏无关 |
| monochrome 层 | 自适应 XML 里已声明，但指向同一个模板前景 drawable |

> ⚠️ **关键约束**：`minSdk = 24`，因此 **API 24–25 的设备读取的是 `mipmap-*dpi/*.webp` 光栅**，
> 而**不是**自适应矢量。只替换矢量的话，老设备上仍是原来的机器人图标。
> 换图标必须**同时**产出矢量与光栅两轨。

### 1.2 启动白屏（需求 ②）

| 项 | 现状 |
|---|---|
| 窗口主题 | `values/themes.xml` 仅 `<style name="Theme.LianLianKan" parent="android:Theme.Material.Light.NoActionBar" />`，**未设置 `windowBackground`**，也无任何启动画面配置 |
| 启动画面库 | **未引入** `androidx.core:core-splashscreen` |
| `activity-compose` | 1.13.0（已足够支持 `installSplashScreen()` 所需的生命周期） |
| 深色主题 | `values-night/themes.xml` 已在 V1.3 删除（深色模式整体移除） |

**白屏根因**：冷启动时系统先绘制窗口主题的 `windowBackground`（平台默认是白色），
Compose 的首帧要等 `onCreate` → `setContent` → 首次组合完成才出现。
这段空档显示的就是那块白底，没有品牌信息、也没有过渡。

**还有第二处闪烁（容易被忽略）**：`MainActivity` 目前这样写：

```kotlin
val settings by settingsRepository.settings.collectAsStateWithLifecycle(initialValue = Settings())
```

`Settings()` 的默认 `themePaletteId = 0`。**若玩家选的是其他主题色，冷启动会先按默认浅蓝渲染，
读到 DataStore 的真实值后再跳到所选配色** —— 这是一次可见的颜色跳变，与白屏叠加在一起。

因此「启动白屏」实际由两个独立成因构成，计划中分别处理。

---

## 2. 需求 ①：APP 图标

### 2.1 设计构想（待确认，见 §9 Q1）

核心玩法是「两块图案相同的牌，用**不超过 2 个拐点**的折线连通」。图标直接编码这件事：

```
┌─────┐         ┌─────┐
│  ●  │         │  ●  │       两块同图案的圆角牌，
└──┬──┘         └──┬──┘       由一条两折连线相接
   └──────┬───────┘
          └──────────  ← 折线（2 个拐点）
```

三种备选（§9 Q1 里选）：
- **A（推荐）**：两块同图案圆角牌 + 一条两折连线。**直接表达玩法**，缩到 48dp 仍可辨识
- **B**：单块牌内嵌一条折线。更简洁，但「连连看」的语义弱一些
- **C**：四个圆点排成方形、两条线连接成「连」的意象。抽象、好看，但看不出是游戏

### 2.2 单一来源生成器（推荐做法）

设计要在**两处**落地（矢量 + 光栅），若各写一份必然漂移。因此新增
`tools/generate_icon.py`（纯标准库，与 `tools/generate_sfx.py` 同一套路数），
**从同一份形状定义同时产出**：

| 产物 | 用途 | 说明 |
|---|---|---|
| `drawable/ic_launcher_background.xml` | 自适应图标背景层 | VectorDrawable，纯色/渐变 |
| `drawable/ic_launcher_foreground.xml` | 自适应图标前景层 | VectorDrawable，108dp 画布、安全区 66dp |
| `drawable/ic_launcher_monochrome.xml` | Android 13+ 主题图标 | 单色剪影（当前 monochrome 层复用了前景，应独立） |
| `mipmap-{m,h,xh,xxh,xxxh}dpi/ic_launcher.png` | API 24–25 方形图标 | 48/72/96/144/192 px |
| `mipmap-{m,h,xh,xxh,xxxh}dpi/ic_launcher_round.png` | API 24–25 圆形图标 | 同上，圆形遮罩 |

**为什么用纯标准库写 PNG**：PNG 就是 zlib 压缩的扫描行 + 若干 chunk，用 `zlib`+`struct`
即可写出，不需要 Pillow。栅格化用**超采样**（例如 4× 渲染再降采样）做抗锯齿，
避免边缘锯齿。这与项目此前用脚本自制音效素材（`generate_sfx.py`）的做法一致，
产物可重复生成、可 diff 审查。

**旧文件清理**：10 个模板 `.webp` 删除，改为同名 `.png`。

### 2.3 图标配色（待确认，见 §9 Q2）

**硬约束：启动图标无法跟随 APP 内的主题色设置。** 自适应图标是静态资源，
运行期改不了（除非用 `activity-alias` 切换，代价大且会在桌面上留下多个入口）。
因此图标只能用**一套固定品牌色**。

建议：用默认主题色（浅蓝 `#8FD3F6`）的 `primary`（tone 40）作为牌面主色，
配 M3 的 `onPrimary`/容器色作对比 —— 与 APP 默认观感一致。

---

## 3. 需求 ②：启动白屏

### 3.1 方案总览

引入 **`androidx.core:core-splashscreen`**（官方向后兼容库，API 24 起可用）：

1. **新增启动主题** `Theme.LianLianKan.Starting`（parent = `Theme.SplashScreen`），配置：
   - `windowSplashScreenBackground`：启动底色
   - `windowSplashScreenAnimatedIcon`：新图标（需求 ① 的产物）
   - `postSplashScreenTheme`：`@style/Theme.LianLianKan`（退出启动画面后切回应用主题）
   - `windowLightStatusBar = true`：与 V1.3 的边到边 + 深色状态栏图标一致
2. **清单**把 `android:theme` 指向启动主题
3. **`MainActivity`** 在 `super.onCreate` 之前调用 `installSplashScreen()`
4. **按住启动画面，直到设置读完**（解决 §1.2 的第二处闪烁）：

```kotlin
val splash = installSplashScreen()
splash.setKeepOnScreenCondition { !settingsLoaded }
```

需要一个「设置已读到首个真实值」的信号。`SettingsRepository.settings` 是 `Flow`，
建议在仓库层补一个 `val isLoaded: StateFlow<Boolean>`（或用一个 `Flow<Settings>` 的
`onEach { loaded = true }` 包装），**不要**用 `delay` 之类的定时猜测。
再加一个兜底超时（例如 1.5s），避免 DataStore 异常时启动画面卡死 —— 这是必须的，
否则「读不到设置」会变成「应用起不来」。

### 3.2 启动底色怎么选（一处需要判断的地方）

启动画面的底色在**主题里写死**，而 APP 的底色由 4 套配色之一生成的 `background` 决定 ——
启动时还不知道玩家选了什么。

好在：M3 的**中性色受种子色影响很小**（4 套配色的 `background` 都是带极轻色相的近白色），
因此取一个固定近中性色即可，出屏时的色差肉眼几乎不可见。

**推荐**：取某个中性 `background`（如浅蓝主题的 `background`）作为固定值，
并在代码注释里写明「4 套配色的 background 差异极小，故固定值不会造成可见跳变」。
若实测发现有色差，备选方案是把启动底色改成**与图标一致的品牌色**（明显的品牌色块出屏），
反而比「接近但不完全一致」更自然。

---

## 4. 风险

| 编号 | 风险 | 影响 | 应对 |
|---|---|---|---|
| R-1 | 矢量与光栅两轨漂移（改了矢量忘了重生成光栅） | 中 —— 老设备显示旧图标 | 单一来源生成器（§2.2）；并在 README 写明「改动图标必须重跑 `generate_icon.py`」 |
| R-2 | 纯 Python 栅格化的抗锯齿质量不如专业工具 | 低 —— 小尺寸下可能略有毛边 | 超采样 4×；真机确认 48dp 下的观感 |
| R-3 | `setKeepOnScreenCondition` 依赖 DataStore 首值，异常时可能卡死启动画面 | **高 —— 应用起不来** | 必须加超时兜底（§3.1 第 4 点）；超时后强制放行 |
| R-4 | 启动底色与所选主题的 `background` 存在细微色差 | 低 | 固定近中性色（§3.2）；真机在 4 套主题下各看一次 |
| R-5 | 自适应图标安全区裁切（前景超出 66dp 安全区会被切掉） | 中 —— 图标被削边 | 前景设计限制在中心 66dp 圆内；真机用「设置 → 壁纸」或长按图标查看多种遮罩形状 |
| R-6 | 图标无法跟随主题色，与 APP 内观感不一致 | 低（且不可解） | 用默认主题色作为固定品牌色（§2.3）；在 README 记为已知约束 |

---

## 5. 验收清单

### 自动（编码侧可完成）

| 编号 | 内容 |
|---|---|
| A-1 | `generate_icon.py` 可重复执行且结果确定；产出的 XML 与 PNG 均被 Android 构建识别（`assembleDebug` 通过） |
| A-2 | 光栅图标尺寸正确（48/72/96/144/192）且为正方形/正圆 |
| A-3 | 全量 JVM 单测全绿（当前 189）；若有新增逻辑（如设置加载状态）需补测 |
| A-4 | `assembleRelease` 成功，release 签名密钥不变 |

### 真机（需开发者确认）

| 编号 | 内容 |
|---|---|
| V-1 | 桌面/应用列表图标为新设计（**且在 API 24–25 设备上也正确**，若手边有） |
| V-2 | 长按图标或更换壁纸，确认**自适应遮罩**（圆/方/泪滴）下均不被裁切 |
| V-3 | Android 13+ 开启「主题图标」后，monochrome 层正常显示为单色剪影 |
| V-4 | **冷启动不再出现白屏**：点图标后有品牌启动画面，过渡到主菜单无突兀色块 |
| V-5 | **4 套主题色下各冷启动一次**：启动底色与 APP 首帧之间无明显色差 |
| V-6 | 上次选了非默认主题色时冷启动：**不再出现「先默认色再跳到所选色」的闪变** |
| V-7 | 启动画面不会长期停留（正常应在数百毫秒内退出）；连续冷启动 5 次均正常 |
| V-8 | 回归：V1.3 的边到边与状态栏图标（浅色背景下深色图标）在启动画面上同样成立 |

---

## 6. 提交规划

```
feat(tools):  新增 generate_icon.py，从单一来源产出矢量与光栅图标
feat(ui):     替换启动图标（自适应 + 光栅 + monochrome）
fix(ui):      引入 core-splashscreen，消除启动白屏与配色跳变
docs:         README 与 V1.4 实施结果
```

`versionCode = 5`、`versionName = "1.4"` 在最后一个功能提交中一并更新。

---

## 7. 工作量

约 **1.5–2 人日**。分解：图标设计（形状定稿）≈ 2h；`generate_icon.py`（PNG 写出 +
超采样栅格化 + VectorDrawable 生成）≈ 4h；启动画面接入与设置加载门控 ≈ 3h；
测试与文档 ≈ 2h。**主要成本在生成器**，图标本身不复杂。

---

## 8. 与需求的对应

| 需求 | 对应 SRS | 说明 |
|---|---|---|
| APP 图标 | SRS **5.1**（界面清单）、**FR-13.7**（图案素材） | 图标属视觉资产，SRS 未单列要求 → 需在 V1.5 的 5.1 补一行 |
| 启动白屏 | 无直接对应 | 属「作品集观感」类改进；建议在 SRS 的 NFR-1（可用性）下补一条「启动过程无空白屏」 |

**SRS 需发布 V1.7**：5.1 补图标要求；NFR-1 补启动体验要求。

---

## 9. 待确认项

### Q1：图标用哪个概念？

- **A（推荐）**：两块同图案圆角牌 + 一条两折连线 —— 直接表达「≤2 拐点连通」的核心玩法
- **B**：单块牌 + 一条折线 —— 更简洁，语义稍弱
- **C**：四点两线的抽象构成 —— 好看但看不出游戏类型

### Q2：图标配色 → **用默认主题色（浅蓝）的 `primaryContainer`**

**这个选择顺带解决了一个问题**：V1.3 的已知问题「`primary` 偏暗」（见 V1.3 计划 §12）
本会影响图标观感 —— 若选 `primary`（tone 40），图标会用上那个偏暗的色。
而 `primaryContainer` 是 **tone 90 的浅色**，本身就是明快的，因此图标观感不受该问题拖累。

**落地细节**：

| 层 | 用色 | 说明 |
|---|---|---|
| 自适应背景层 | `primaryContainer`（浅蓝 tone 90） | 品牌底色，明快 |
| 牌面填充 | `primary`（tone 40） | 与浅色底形成对比，保证 48dp 下可辨识 |
| 牌内图案点 | `primaryContainer` | 让「图案」在深色牌面上可见 |
| 两折连线 | `primary`（tone 40） | 与牌面同色，读作一体 |

> ⚠️ **一处必须注意的实现细节**：`primaryContainer` / `primary` 都是**运行期由 M3 算法生成**的，
> 而图标是**静态资源**。因此需要先用一个临时测试把这两套色值 dump 出来，
> 把**字面 hex** 写进图标生成器，并在生成器里注明「来源是 `ThemePalettes.byId(0).light.*`，
> 若日后改动种子色或 tone 覆盖，需重新 dump」。否则图标颜色会与 APP 悄悄脱节。

---

**两项已确认，可动手实施。**

---

## 10. 实施结果（2026-09-12）

**状态**：两项需求均已实施完毕。`assembleDebug` / `assembleRelease` / `testDebugUnitTest` 三条命令全绿
（**194** 个 JVM 单测，0 失败 / 0 错误；基线 189 + 本次新增 5），版本 `versionCode = 5`、`versionName = "1.4"`。
真机验收项见 §10.6。

### 10.1 产物清单

| 产物 | 说明 |
|---|---|
| `tools/generate_icon.py` | **单一来源生成器**（纯标准库 `zlib`/`struct`/`math`，纯 ASCII），一次产出以下全部图标文件 |
| `drawable/ic_launcher_background.xml` | 自适应背景层：`primaryContainer` 填满 108×108 |
| `drawable/ic_launcher_foreground.xml` | 自适应前景层：两块圆角牌 + 图案点 + 两折连线 |
| `drawable/ic_launcher_monochrome.xml` | Android 13+ 主题图标：单色剪影，牌内图案点用 `evenOdd` 挖成**孔** |
| `mipmap-{m,h,xh,xxh,xxxh}dpi/ic_launcher.png` | 光栅方形图标：48 / 72 / 96 / 144 / 192 px |
| `mipmap-{m,h,xh,xxh,xxxh}dpi/ic_launcher_round.png` | 同上，圆形遮罩（透明区内 alpha=0） |

生成器自带两条自检，越界即 `SystemExit` 报错退出：① 形状最外点到画布中心的距离 ≤ 33（66dp 安全圆半径）；
② 每个 PNG 的边长等于目标档位。

**确定性**：连续运行两次，13 个产出文件（3 个 XML + 10 个 PNG）SHA-256 **逐一相同**。
抗锯齿用 4× 超采样（每个尺寸族渲染一次 4× 图再整数盒降采样，等价且省算力）；
光栅构图按最大边占画布 **72%** 居中缩放，圆形版同比例、只换圆形遮罩以保持视觉重量。

### 10.2 dump 出的色值（Q2 的落地依据）

| 角色 | ARGB | 图标中的用途 |
|---|---|---|
| `primary` | `0xFF126684` | 牌面填充、两折连线 |
| `primaryContainer` | `0xFF8FD3F6` | 自适应背景层、牌内图案点 |
| `background` | `0xFFF8F9FC` | 启动画面底色、光栅图标底色 |

（顺带记录：默认配色的 `surface` 也是 `0xFFF8F9FC`。4 套配色的 `background` 实测为
`#F8F9FC`（浅蓝）/ `#FAFBE8`（黄绿）/ `#FFF8F7`（玫红）/ `#FFF8F3`（琥珀），
两两之间**单个通道最大差 20/255**（浅蓝×黄绿的蓝通道）——  §3.2 的
「固定一个近中性色即可、差异肉眼不可见」由此得到实测支持。）

这三个字面值写在 `tools/generate_icon.py` 顶部，注释里注明来源是
`ThemePalettes.byId(0).light`（运行期由 M3 色调板算法生成）；**改动种子色或 tone 覆盖后
必须重新 dump 并更新该文件**，否则图标颜色会与 APP 悄悄脱节。

### 10.3 图标几何（108×108 自适应画布）

| 元素 | 尺寸 | 说明 |
|---|---|---|
| 牌 | 22 × 22，圆角 **6.8** | 圆角约为边长的 31%，缩到 48dp 仍明显是「圆角牌」 |
| 两牌间距 | 8.4 | 水平并排，整体居中 |
| 牌内图案点 | 半径 4.2 | 位于牌心，牌面与点正好是 Q2 指定的两种色 |
| 两折连线 | 宽 6.2，圆头圆角 | 从两牌**底边内侧 5.2** 处下探 **11.0**，中间走一段水平线 —— 恰好 **2 个拐点** |

- 组合包围盒 **52.4 × 36.1**，几何中心 (54, 54)。
- **安全区**：最外点（两块牌的圆角外侧）到画布中心 **29.23**，安全圆半径 33，余量 **3.77**
  ⇒ 同时落在 66×66 安全方框 `[21, 87]²` 内，圆心形/方形/泪滴形遮罩都不会切到（对应 R-5）。
- 连线并不真的「落空」：它以同色起于牌内、穿过牌底边圆角，因此缩到 48px 也不会出现接缝。

### 10.4 改动 / 新增 / 删除的文件

**新增**

| 文件 | 用途 |
|---|---|
| `tools/generate_icon.py` | 图标单一来源生成器 |
| `drawable/ic_launcher_monochrome.xml` | 主题图标单色层（原先误用前景） |
| `values/colors.xml` | 启动底色 `splash_background`（该文件 M0 被删过，本次重建） |
| `ui/SplashGate.kt` | 启动画面放行门控（可单测） |
| `test/.../ui/SplashGateTest.kt` | 5 个用例，覆盖正常路径与超时兜底（R-3） |
| `mipmap-*dpi/ic_launcher{,_round}.png` ×10 | 光栅图标 |

**修改**

| 文件 | 改动 |
|---|---|
| `gradle/libs.versions.toml` | 新增 `coreSplashscreen = "1.2.0"` 与 `androidx-core-splashscreen` |
| `app/build.gradle.kts` | 加依赖；`versionCode 4→5`、`versionName "1.3"→"1.4"` |
| `values/themes.xml` | 新增 `Theme.LianLianKan.Starting`（`Theme.LianLianKan` 本身未动） |
| `AndroidManifest.xml` | `MainActivity` 的 `android:theme` 指向启动主题 |
| `MainActivity.kt` | `super.onCreate` 前 `installSplashScreen()` + 放行条件；首次组合改用抓到的真实设置 |
| `data/SettingsRepository.kt` | 接口加 `isLoaded: StateFlow<Boolean>`；内存实现恒 true |
| `data/DataStoreRepositories.kt` | 首值读到的瞬间置 `isLoaded = true`（含预热作用域） |
| `drawable/ic_launcher_{background,foreground}.xml` | 由模板素材替换为新图标 |
| `mipmap-anydpi-v26/ic_launcher{,_round}.xml` | `<monochrome>` 改指 `@drawable/ic_launcher_monochrome` |
| `README.md` | 版本表、已知取舍、素材生成一节、测试数 189→194 |
| `doc/V1.4改进计划.md` | 状态行 + 本节 |

**删除**：`mipmap-{m,h,xh,xxh,xxxh}dpi/ic_launcher.webp` 与 `ic_launcher_round.webp`（共 10 个模板光栅）。

### 10.5 与计划的差异

1. **超出计划：放行逻辑抽成 `ui/SplashGate.kt`。** 计划只要求「加超时兜底」，但把这段留在
   `MainActivity` 里意味着 R-3（等级「高」）只能靠真机验收。抽成挂起函数后，「读到设置就放行」
   与「读不出来时超时放行」两条分支都进了 JVM 单测（`SplashGateTest`）。
2. **超出计划：首次组合直接用抓到的真实设置。** 计划只要求「设置读完前不放行」，但仅靠不放行
   仍可能有一帧默认配色（`collectAsStateWithLifecycle` 的 `initialValue` 要到流发出才更新）。
   现在 `MainActivity` 把 [SplashGate] 取回的真实 `Settings` 交给首次组合，第二次闪烁才算彻底消除。
3. **偏离：`DataStoreSettingsRepository` 多了一个带默认值的构造参数 `preloadScope`。**
   放行条件在 `setContent` **之前**就被系统每帧询问，仓库必须自己先把首值读出来，
   因此需要一个作用域；暴露成参数是为了让测试注入 `TestScope`，不依赖真实 IO 线程时序。
4. **偏离：生成的三个图标 XML 用英文注释。** 编码约定要求 `tools/*.py` 纯 ASCII，而中文注释
   会让脚本出现非 ASCII 字节。中文说明放在本文件、README，以及人写的
   `values/colors.xml`、`values/themes.xml`、`AndroidManifest.xml` 里。
5. **踩坑记录：XML 注释内不能出现连续两个短横线。** 第一版生成器写了
   `Generated by ... -- do not edit`，`mergeDebugResources` 直接
   `XMLStreamException: ParseError at [row,col]:[3,41]` 失败。生成器里已写明这条约束。
6. **实现细节：monochrome 把图案点挖成孔而不是画第二色。** 主题图标由系统单色着色，
   「第二种颜色」没有意义；用 `fillType="evenOdd"` 保留「牌上有图案」的信息。
7. **顺带发现（已记入 README 已知取舍）：启动图标会偏小。** 启动主题按需求复用自适应前景，
   而 API 31+ 会把这张 108dp 画布放进 192dp 的可见圆里；前景内容本就只占画布 61%
   （安全区所致），因此启动画面上的图标比常见 APP 小一些。真机若观感不佳，
   可另出一张放大版启动图标（不影响自适应图标）。

### 10.6 仍需真机验收（§5 的 V-1 ~ V-8）

编码侧能验的都已验（A-1 ~ A-4：生成器确定性、PNG 尺寸、194 单测全绿、`assembleRelease` 签名密钥未变），
以下 8 项**只能**在设备上确认：

| 编号 | 内容 | 本次已做的准备 |
|---|---|---|
| V-1 | 桌面/应用列表图标为新设计（API 24–25 设备也正确） | 光栅轨已产出 10 个 PNG 并从模板 webp 切换过去 |
| V-2 | 自适应遮罩（圆/方/泪滴）下不被裁切 | 几何自检保证最外点半径 29.23 < 33 |
| V-3 | Android 13+「主题图标」显示为单色剪影 | monochrome 层独立产出，图案点为孔 |
| V-4 | 冷启动不再白屏 | 启动主题 + `installSplashScreen()` |
| V-5 | 4 套主题色下启动底色无明显色差 | 固定 `#F8F9FC`，与 4 套 `background` 实测一致 |
| V-6 | 上次选了非默认主题色时不再闪变 | 读完设置才放行，且首次组合用真实设置 |
| V-7 | 启动画面不长期停留（连续冷启动 5 次正常） | 超时兜底 1.5s 已单测覆盖 |
| V-8 | 边到边与深色状态栏图标在启动画面上同样成立 | 启动主题已设 `android:windowLightStatusBar = true` |
