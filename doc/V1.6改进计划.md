# V1.6 改进计划：关于界面

| 项 | 内容 |
|---|---|
| 文档版本 | V1.0（计划草案） |
| 编制日期 | 2026-09-14 |
| 基线版本 | V1.5（`versionCode = 11`、tag `v1.5.0`） |
| 依据 | 开发者需求：**添加关于信息显示界面，显示 APP 名称、版本号、版权信息** |
| 计划交付 | `versionCode = 12`、`versionName = "1.6"` |
| 状态 | **已实施（2026-09-14）—— 待真机验收**；4 个待确认项已确认（见 §13），实施结果见 §14 |

---

## 1. 背景与目标

V1.5 已定稿（`v1.5.0`）。本版本新增一个**关于界面**，用于展示应用自身的身份信息：
APP 名称、版本号、版权信息。

三类典型用途决定了它不是一个「可有可无」的页面：

1. **作品集场景**：本项目定位是个人作品/作品集（SRS 1.2），评审者或协作者拿到 APK 后，
   第一件想知道的事就是「这是什么、哪个版本、谁做的」。
2. **问题反馈**：真机返工时最常缺的信息是版本号（V1.5 一轮返工就吃过这个亏 —— 当时无法
   确认设备上装的是哪一版，只能靠 `versionCode` 逐版递增来区分）。
3. **归属声明**：版权信息是作品的归属标识。

### 1.1 与既有需求的关系

SRS 5.1 的界面清单目前有：主菜单、启动图标、关卡选择、游戏主界面、暂停弹窗、结算弹窗、
设置界面 —— **没有关于界面**。因此本版本是一次**需求新增**，需同步 SRS（见 §6.1）。

---

## 2. 现状勘察（全部核对过源码与文档）

| 事项 | 现状 | 对本版本的影响 |
|---|---|---|
| SRS 界面清单（5.1） | 无「关于界面」行 | 需新增一行（V1.11） |
| SRS 功能需求 | FR-1 ~ FR-14，无对应条目 | 需新增 **FR-15 关于**（V1.11） |
| 设置界面结构 | `SwitchRow`（音效/振动）→ 主题色色块 → 皮肤卡片 → 「清除进度」`TextButton` | 「关于」入口的自然落点是「清除进度」下方（待确认项 Q1） |
| APP 名称 | `R.string.app_name` = 「连连看」，同时用于桌面图标与主菜单标题 | 直接复用，**不新增**一份名称字符串（避免两处不一致） |
| **版本号可获取性** | **`app/build.gradle.kts` 只开了 `buildFeatures { compose = true }`，`BuildConfig` 未启用** —— 代码里目前**拿不到** `versionName` / `versionCode` | 需在 §4.1 选一条取版本的路径 |
| 路由 | `Route`（`ui/nav/AppNavHost.kt`）有 menu / levels / settings / game | 需新增 `about` 路由 + 一个 composable 目标 |
| 权限 | 仅 `VIBRATE`（AC-20、NFR-6.1） | 本版本**不得引入任何新权限**（含网络），见 §3 |
| 版权/作者信息 | 仓库与文档中没有任何「版权」表述；提交者署名 `leid <ld-1@163.com>` | 版权文字**必须由开发者给定**（Q3），不可由实现方臆造 |
| 背景 | V1.5 的动态背景铺在**所有界面之下**（`MainActivity` 的 `Box` 第一子级） | 关于界面**不得画不透明底色**，否则背景整层被盖住（V1.5 §15.1 的真实事故） |

---

## 3. 设计约束

| # | 约束 | 来源 |
|---|---|---|
| 1 | **不新增任何权限**，尤其不得为了「检查更新」引入网络权限 | SRS NFR-6.1 / NFR-6.3 / AC-20；FR-12.4「不含 BGM」同源的约束思路 |
| 2 | **不新增第三方依赖** | 本版本只是展示静态信息，无此必要（也符合「依赖清单定稿」的基线） |
| 3 | 关于界面**必须透明覆盖在动态背景之上**，不得画不透明满屏底色 | V1.5 计划 §15.1（两屏 `Scaffold` 默认底色把背景整层盖住的事故） |
| 4 | 版本号**必须来自构建产物**，不得在界面代码里写死 | 否则改版本号时必然漏改一处 |
| 5 | 文案一律走 `strings.xml`，不在 Compose 里写死中文字面量 | 既有约定（M6 起全部文案资源化） |
| 6 | 文本对比度沿用主题角色（`onSurface` / `onSurfaceVariant`），不引入自定义颜色 | `ThemePaletteContrastTest` 已对 4 套配色逐对断言 WCAG 对比度 |
| 7 | 分层不变：`domain/` 不得 import `android.*` / `androidx.*` | SRS NFR-3.1（每个里程碑都复核） |
| 8 | 必须支持系统返回键与顶部返回按钮 | 与关卡选择、设置界面一致 |

---

## 4. 技术方案

### 4.1 版本号从哪来（本版本唯一有技术含量的决策）

| 方案 | 做法 | 优点 | 缺点 |
|---|---|---|---|
| **A（推荐）** | 打开 `buildFeatures { buildConfig = true }`，读 `BuildConfig.VERSION_NAME` / `VERSION_CODE` | 编译期常量；无 API 弃用问题；`BuildConfig.DEBUG` 顺带可用于区分构建类型；纯 JVM 单测可直接断言 | 需要改一处构建配置（一行） |
| B | `PackageManager.getPackageInfo(packageName, 0)` | 不改构建配置；反映**已安装 APK**的真实版本 | API 33+ 该重载被弃用，需按版本分支；需要 `Context`，单测里无法直接覆盖 |
| C | 在 `res/values/` 里放一个手写的 `version_name` 字符串 | 最简单 | **违反约束 4**，直接排除 |

取 **A**。理由：本项目的版本号唯一真源是 `app/build.gradle.kts`，`BuildConfig` 正是它的投影；
`versionName` 与 `versionCode` 同时展示还能顺带解决「设备上装的是哪一版」这个 V1.5 遇到过的问题。

### 4.2 展示内容与排版

```
┌─────────────────────────────┐
│ ← 关于                       │  TopAppBar（透明，复用既有样式）
├─────────────────────────────┤
│                             │
│         [ 应用图标 ]          │  自适应图标的前景层（矢量，任意尺寸不糊）
│          连连看               │  R.string.app_name（与桌面图标同名）
│       版本 1.6（构建 12）      │  BuildConfig.VERSION_NAME / VERSION_CODE
│                             │
│   一句话简介（可选，Q2）        │
│                             │
│  ─────────────────────────  │  HorizontalDivider
│   版权行（Q3 给定文字）         │
│   技术栈/致谢（可选，Q2）       │
└─────────────────────────────┘
```

- 文字全部使用主题角色：名称 `headlineSmall` + `onSurface`，版本 `bodyMedium` + `onSurfaceVariant`，
  版权 `bodySmall` + `onSurfaceVariant`。
- 图标用 `painterResource(R.drawable.ic_launcher_foreground)`（V1.4 生成的自适应图标前景层，
  已按 66dp 安全区作画，因此任何尺寸都不裁切）—— 不额外生成资源。

### 4.3 代码结构

| 文件 | 动作 | 说明 |
|---|---|---|
| `ui/about/AboutScreen.kt` | 新增 | 界面本身；只接收一个 `AppInfo`，不做任何取版本的动作（便于预览与测试） |
| `ui/about/AboutInfo.kt` | 新增 | `AppInfo` 数据类 + `appInfoFromBuild()` + 纯函数 `aboutVersionLabel(name, code, debug)`；**纯 Kotlin，不 import `android.*`** |
| `ui/nav/AppNavHost.kt` | 改 | 新增 `Route.ABOUT` 与对应 composable 目标，把 `onAbout` 回调传给设置界面 |
| `ui/settings/SettingsScreen.kt` | 改 | 底部新增「关于」入口行（Q1 = A 时） |
| `ui/menu/MenuScreen.kt` | 改 | 仅在 Q1 = B/C 时改动 |
| `res/values/strings.xml` | 改 | 新增 `menu_about` / `about_version_label` / `about_copyright` 等 |
| `app/build.gradle.kts` | 改 | `buildConfig = true`；`versionCode = 12`、`versionName = "1.6"` |
| `app/src/test/.../ui/about/AboutInfoTest.kt` | 新增 | 见 §5 |

**关于 `domain/`**：本版本不碰 `domain/` —— 版本号与版权都不是业务领域概念，属展示层信息。

---

## 5. 测试方案

### 5.1 自动（JVM 单测）

| 编号 | 断言 | 目的 |
|---|---|---|
| A-1 | `aboutVersionLabel` 对正常输入返回 `"1.6（构建 12）"`；对空版本名 / 0 或负数构建号等退化输入不抛异常且仍有可读输出 | 展示逻辑的正确性与健壮性 |
| A-2 | **`aboutVersionLabel` 的输出必须包含 `BuildConfig.VERSION_NAME`** | 防「把版本号写死在代码里」：一旦硬编码，下次改版本时这条会红 |
| A-3 | `appInfoFromBuild()` 的名称/版本非空，且 `versionName` 形如 `数字.数字` | 防构建配置漏填 |

基线 203 条 → 预计 **206** 条。

### 5.2 真机手动验收（V 项）

| 编号 | 内容 |
|---|---|
| V-1 | 从设置界面底部能进入「关于」；**顶部返回按钮与系统返回键都能正常返回**，返回后设置界面状态不变 |
| V-2 | 名称显示为「连连看」，**与桌面图标下方的名称一致** |
| V-3 | 版本号显示为 `1.6（构建 12）`，与安装包的 `versionName`/`versionCode` 一致（可用 `adb shell dumpsys package com.ldxy.lianliankan` 核对） |
| V-4 | 版权信息完整显示、不被截断；长行按预期换行 |
| V-5 | **4 套主题色 × 3 套皮肤下各看一眼**：文字对比度足够，且**动态背景在关于界面仍然可见**（验证没有被不透明底色盖住） |
| V-6 | 系统字体/显示大小调到最大后，名称、版本、版权三行仍完整可读、不截断 |
| V-7 | 回归：设置界面原有功能（音效/振动/主题色/皮肤/清除进度）不受影响；主菜单与其它界面正常 |

### 5.3 已由既有自动化覆盖、本版本无需重测

- AC-01 ~ AC-20：本版本不触碰游戏逻辑、算法、存储与既有界面行为（V-7 作回归确认）。
- 权限：仍只有 `VIBRATE`（构建后用 `aapt2 dump badging` 逐项核对，纳入实施记录）。
- 主题对比度：`ThemePaletteContrastTest` 已覆盖所用角色。

---

## 6. 文档与需求变更

### 6.1 SRS 发布 V1.11

| 位置 | 变更 |
|---|---|
| 修订历史 | 新增 V1.11 行 |
| 5.1 界面清单 | 新增「关于界面」一行：**展示 APP 名称、版本号与版权信息；从设置界面进入** |
| 功能需求 | 新增 **FR-15 关于**：`FR-15.1` 显示 APP 名称（与启动器名称一致）`FR-15.2` 显示版本号（取自构建产物，不得硬编码）`FR-15.3` 显示版权信息 `FR-15.4` 入口位于设置界面（按 Q1 结论）`FR-15.5` **不联网、不申请任何新权限、不请求更新** |

### 6.2 其它文档

- `doc/开发计划.md`：需求覆盖矩阵补 FR-15；新增「V1.6」一节（先记「已实施，待真机验收」，验收后补定稿）。
- `README.md`：版本表新增 V1.6 行；目录结构补 `ui/about/`；测试条数更新。
- 本文档：实施后追加「实施结果」与「定稿」两节（沿用 V1.5 §19 的写法）。

---

## 7. 风险表

| 编号 | 风险 | 等级 | 应对 |
|---|---|---|---|
| R-1 | **关于界面用了不透明底色，把动态背景整层盖住** | 中 | 约束 3；`Scaffold` 显式传 `containerColor = Color.Transparent`；V-5 逐屏确认（这是 V1.5 真实踩过的坑） |
| R-2 | 版本号与构建产物不一致（写死、或忘改） | 中 | 约束 4 + A-2 断言；V-3 与 `dumpsys` 核对 |
| R-3 | 版权文字由实现方臆造，与开发者本意不符 | 中 | 版权文字**必须**由开发者给定（Q3）；未给定前不实施 |
| R-4 | 打开 `buildConfig = true` 后 `BuildConfig` 体积/影响 | 低 | 只生成一个常量类，无运行时开销 |
| R-5 | 长文案在窄屏或大字号下截断 | 低 | 文案保持简短；V-6 用最大字号验收 |
| R-6 | 入口位置改变主菜单按钮数量，影响既有验收观感 | 低 | 默认放设置界面（Q1 = A），主菜单不动 |
| R-7 | 图标在多套主题色下显得突兀（图标固定用浅蓝生成，不随主题变色） | 低 | 这是 V1.4 已记录的已知限制；关于界面只是复用同一资源，不新增问题 |

---

## 8. 任务分解

| # | 任务 | 产出 |
|---|---|---|
| T-1 | SRS 发布 V1.11（5.1 + FR-15） | `doc/软件需求规格说明书.md` |
| T-2 | 打开 `buildConfig`，bump 版本号到 12 / "1.6" | `app/build.gradle.kts` |
| T-3 | `AppInfo` + `aboutVersionLabel` 纯函数，配套单测 | `ui/about/AboutInfo.kt`、`AboutInfoTest.kt` |
| T-4 | `AboutScreen` 界面 | `ui/about/AboutScreen.kt` |
| T-5 | 路由 + 入口（设置界面底部） | `AppNavHost.kt`、`SettingsScreen.kt` |
| T-6 | 文案资源 | `res/values/strings.xml` |
| T-7 | 文档同步（开发计划矩阵、README） | 相应文件 |
| T-8 | 构建、单测、`aapt2` 权限核对、实施记录 | 本文档 §14 |

---

## 9. 提交计划

沿用既有粒度，每步一次提交：

1. `docs: 制订 V1.6 改进计划（关于界面）` ← 本文档
2. `docs: V1.6 计划固化 N 项决策`（§13 定稿后）
3. `feat: V1.6 关于界面（名称 / 版本号 / 版权信息）`
4. `docs: V1.6 实施结果与文档同步`
5. 真机验收通过后：`docs: V1.6 定稿` + tag `v1.6.0` + `dist/LianLianKan-v1.6.0-release.apk`

---

## 10. 工作量估算

约 **0.5 人日**。分解：SRS 与文案 ≈ 1h；`AppInfo` + 单测 ≈ 1h；界面与入口 ≈ 1.5h；文档与验证 ≈ 1h。
本版本没有算法与性能问题，主要成本在**文案定稿**（要等 Q3）与真机验收。

---

## 11. 明确不做的事

| 事项 | 原因 |
|---|---|
| 检查更新 / 版本提示 | 需要网络权限，直接违反 NFR-6.1 / AC-20 |
| 评分、分享、反馈邮件 | 本版本需求只提「显示」；且会引入 Intent 与外部应用依赖 |
| 开源许可全文页（逐库列出 Apache-2.0 全文） | 属另一件事（合规页），需要逐依赖整理；本版本至多做一行简短致谢（见 Q2） |
| 多语言 | 项目至今只有 `values/`（中文）；引入 i18n 是独立议题 |
| 「最近更新内容」/ 更新日志页 | 无既有数据来源，需另建维护流程 |

---

## 12. 验收标准映射

| 需求 | 自动化 | 真机 |
|---|---|---|
| FR-15.1 名称 | — | V-2 |
| FR-15.2 版本号 | A-1 / A-2 / A-3 | V-3 |
| FR-15.3 版权 | — | V-4 |
| FR-15.4 入口 | — | V-1 |
| FR-15.5 不联网/无新权限 | 构建产物权限核对（§5.3） | V-7 |
| 约束 3（背景不被盖住） | — | V-5 |
| 约束 6（对比度） | `ThemePaletteContrastTest`（既有） | V-5 |

---

## 13. 待确认项 —— 已确认（2026-09-14）

四项均按建议取值确认，本版本的实施范围据此冻结。

### Q1：入口位置 → **A（设置界面底部）**

「关于」入口放在设置界面最下方，「清除进度」的下方，用一个带尾部箭头的行（`ListItem` 风格）。

> **连带影响**：§8 的 T-5 只改 `SettingsScreen.kt` 与 `AppNavHost.kt`，**`MenuScreen.kt` 不动**
> → §7 的 **R-6 风险随之作废**（主菜单按钮数不变，既有验收观感不受影响）。

### Q2：内容范围 → **A（图标 + 名称 + 版本 + 一句话简介 + 版权行）**

具体展示：

| 元素 | 取值 | 来源 |
|---|---|---|
| 应用图标 | `R.drawable.ic_launcher_foreground`（V1.4 生成的自适应图标前景层） | 复用既有资源，不新增 |
| 名称 | 「连连看」 | `R.string.app_name`（与桌面图标同名，唯一真源） |
| 版本 | 「版本 1.6（构建 12）」 | `BuildConfig.VERSION_NAME` / `VERSION_CODE` |
| 一句话简介 | 拟：「经典连连看：两张同图案的牌，用不超过 2 个拐点的折线相连即可消除。」 | 新增文案，取自 SRS 1.2 的项目背景表述 |
| 版权 | 「© 2026 leid」 | Q3 |

> **不做**：技术栈与开源库致谢（Q2 = C 的内容）、设备与构建信息（Q2 = D 的内容）——
> 两者都保留在 §11 的「明确不做」之外，即日后想做时随时可加，不构成本版本的技术债。

### Q3：版权行的确切文字 → **A「© 2026 leid」**

年份**写死**（不运行时取当前年）：作品集要的是「发布年份」，且同一份 APK 在任何时候
显示的版权信息都应一致。

> **记录一处事实**：这一项必须由开发者给定 —— 版权署名是不可由实现方代拟的信息，
> §7 的 R-3 即为此设。实现时该行写入 `strings.xml`（`about_copyright`），
> 不在 Kotlin 里拼接。

### Q4：联系方式 → **A（不显示）**

界面不显示邮箱或链接，因此本版本**不引入任何 `Intent`**，也不涉及个人信息暴露。

> **连带影响**：§2 约束 1（不新增权限）在实现层是**自然满足**的 —— 整个版本没有任何
> 跳出应用的交互，这一点在实施记录里会以「构建产物权限核对」予以确认（§5.3）。

---

## 14. 实施结果（2026-09-14）

| 项 | 结果 |
|---|---|
| 交付 | `versionCode = 12`、`versionName = "1.6"` |
| 新增 | `ui/about/AboutInfo.kt`（`AppInfo` + `aboutVersionLabel` + `appInfoFromBuild`）、`ui/about/AboutScreen.kt`、`app/src/test/.../ui/about/AboutInfoTest.kt`（3 条） |
| 改动 | `app/build.gradle.kts`（`buildConfig = true` + 版本号）、`ui/nav/AppNavHost.kt`（`Route.ABOUT` + composable 目标 + 给设置界面传 `onAbout`）、`ui/settings/SettingsScreen.kt`（新增 `onAbout` 参数与底部入口）、`res/values/strings.xml`（4 条文案）、SRS（V1.11）、开发计划（覆盖矩阵 + V1.6 一节）、README |
| **未改动** | `domain/**`、`data/**`、`ui/theme/**`、`ui/game/**`、`ui/menu/**` 一律未动；**依赖清单一条未加**（约束 2） |
| 验证 | `assembleDebug` / `assembleRelease` / `testDebugUnitTest` 全部 BUILD SUCCESSFUL；单测 **206** 条、失败 0（基线 203 + 新增 3） |
| 交付物核对 | `aapt2 dump badging`：`versionCode='12' versionName='1.6'`；**权限与 V1.5 逐项一致**（仅 `VIBRATE` + AndroidX 自动加入的 `DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION`）→ 约束 1 与 AC-20 不受影响 |
| 构建配置核对 | `BuildConfig` 实测已生成：`VERSION_CODE = 12`、`VERSION_NAME = "1.6"`、`DEBUG`、`BUILD_TYPE` |

### 14.1 与计划的差异（4 处，都记下来）

| # | 计划 | 实装 | 原因 |
|---|---|---|---|
| 1 | §4.3 写「入口行用 `ListItem` 风格（带尾部箭头）」 | 用一个全宽 `TextButton`（与「清除进度」同款） | 勘察发现**本项目没有引入 Material 图标依赖**（`Icons.*` 全项目零使用），为了一个箭头去加依赖不值得（违反约束 2）。样式改为沿用本页既有写法 |
| 2 | §4.2 写「图标用自适应图标前景层」 | 照做，显示尺寸取 **112dp** | 该画布是 108dp、内容只占中间 66dp，因此同尺寸下看着偏小（V1.4 的启动图标有同一取舍）。备选方案（背景层 + 前景层按 1.636 倍放大 + 圆角裁剪，约六行）已写在 `AboutScreen` 的 KDoc 里，真机若觉得小可直接换 |
| 3 | §5.1 的 A-1 未细化「版本名缺失时构建号是否保留」 | 定为**保留**：`"" + 12` → `「未知版本（构建 12）」` | 版本名缺失时构建号是唯一还能定位到包的信息，丢掉更糟。实施时最初把「省略构建号」写成期望，测试红了之后判定**是断言错、不是代码错**，改断言 |
| 4 | §13 Q2 未提图标尺寸与简介文案 | 简介取 SRS 1.2 的表述改写；图标 112dp | 文案定稿属计划留白项 |

### 14.2 仍需真机验收

`doc/V1.6改进计划.md` §5.2 的 **V-1 ~ V-7**（入口与返回、名称与桌面图标一致、版本号与安装包一致、
版权完整、**4 主题 × 3 皮肤下背景仍可见**、大字号不截断、回归）。
自动化部分（A-1 ~ A-3）已由单测覆盖，权限部分已由构建产物核对覆盖。

**本次特别值得在真机上确认的一条**：V-5 中的「动态背景在关于界面仍然可见」——
新界面是 `Scaffold`，而它在 V1.5 里正是把背景整层盖住的元凶；本次已显式传
`containerColor = Color.Transparent`，但这一类问题**只有肉眼能确认**。

---

## 15. 定稿

（真机验收通过后填写，并打 `v1.6.0` 标签、产出 `dist/LianLianKan-v1.6.0-release.apk`。）
