# V1.5 改进计划

## 连连看游戏（Android）

| 项目 | 内容 |
|---|---|
| 文档版本 | V1.0 |
| 编制日期 | 2026-09-12 |
| 基线版本 | V1.4（`versionCode = 5`） |
| 依据 | 开发者需求：**增加动态背景** |
| 计划交付 | `versionCode = 6`（**修订 1 起为 7**）、`versionName = "1.5"` |
| 状态 | **已实施（修订 1，2026-09-14）—— 待真机验收**；3 个待确认项已于 §13 确认；首次实施结果见 §14，真机反馈「看不出在动」的排查与重做见 §15 |

---

## 1. 范围

**本版本只做一件事：给应用加动态背景。** 另含一项**补做**（见 §2.2）。

明确**不做**（属遗留项，见 §12）：`primary` 偏暗的修复、开启 R8 混淆、皮肤携带配色。

---

## 2. 现状核查

### 2.1 背景与动画现状

| 项 | 现状 |
|---|---|
| 背景 | 单个**静态**竖向渐变：`Theme.kt` 的 `appBackgroundBrush()`，取 `background` → `surfaceVariant(55%)` |
| 应用位置 | 只在 `MainActivity.kt:118` 用一次 —— 一个铺满全屏的 `Box`，所有界面都浮在它上面 |
| 棋盘底板 | **没有**。牌直接浮在渐变上（`BoardView` 里无任何 `background`/scrim） |
| 常驻动画 | **全项目为 0** —— `rememberInfiniteTransition` 用量为 0 |
| 现有动画 | 全是**触发式的有限时长**：连线 150ms、消除 250ms、抖动 320ms |

> ⚠️ **最后一行是本版本最重要的现状**：加入动态背景意味着应用**首次出现永不停止的动画**。
> 游戏界面将不再进入「无动画」状态，功耗与发热特性发生实质变化 —— 这不是改一个 `Brush` 那么简单。

### 2.2 一处需要补做的文档遗漏

**V1.4 的 SRS 修订漏做了。** V1.4 计划 §8 写明「SRS 需发布 V1.7：5.1 补图标要求；
NFR-1 补启动体验要求」，但实施时该条没有进入子代理的任务清单，我在定稿时也没核对 ——
**SRS 至今仍是 V1.6，图标与启动体验两项要求均未写入**。

本版本一并补做（详见 §7）。

---

## 3. 设计约束（核心）

**这是益智游戏：玩家需要持续扫视棋盘找配对。动态背景最大的风险不是性能，而是干扰玩法。**

以下约束是硬性的，会写成代码里的常量并由测试断言：

| 约束 | 取值 | 理由 |
|---|---|---|
| 运动周期 | **≥ 20 s** | 周期太短会形成「节律」，吸引注意力 |
| 元素透明度 | **≤ 0.08** | 背景元素必须弱到「看久了才注意到」 |
| 元素数量 | **≤ 8 个** | 控制绘制预算，也控制视觉噪声 |
| 形状 | **不得为圆角方形，也不得使用皮肤图案** | 避免与真实牌混淆 —— 这是本版本最容易出的错 |
| 可用颜色 | **仅中性色**（`background`/`surfaceVariant`/`outline` 一类） | **不得使用 `primary`/`tertiary`/`error`** —— 它们分别承载选中、提示、错误语义，背景若用同色会抢注意力、甚至造成误判 |

最后一条尤其重要：V1.3 定下的三个强调色各有明确语义（选中/提示/错误），
背景若从中取色，会直接削弱这三个信号的辨识度。

**关于棋盘的隔离**：牌目前直接浮在背景上。若背景开始运动，牌与背景的边界会变得不稳定。
**建议给棋盘加一层极淡的底板**（scrim），既能隔离运动背景、又能提升牌的对比度。
但它会改变 V1.4 刚验收过的棋盘观感，因此列为待确认项（§13 Q2）。

---

## 4. 技术方案

新增 `ui/theme/AnimatedBackground.kt`：

```kotlin
/** 动态背景的全部参数，纯数据 —— 便于断言 §3 的约束。 */
data class BackgroundSpec(
    val blobCount: Int,
    val alpha: Double,
    val periodMillis: Int,
    val colors: List<Color>,
)

@Composable
fun AnimatedBackground(modifier: Modifier = Modifier)
```

实现要点：

1. **用 `Canvas` 的 draw lambda 读动画状态，而不是在组合期读**。
   `Canvas` 底层是 `drawBehind`；在它的 draw block 里读 `State<Float>` 只会让**绘制阶段**失效，
   **不触发重组**。若在组合期读，每帧都会重组一遍整棵子树 —— 这是本项目首次引入常驻动画，
   这点做错代价很大。
2. **参数集中为 [BackgroundSpec]**，形如「N 个柔和圆形，各自按不同相位/速度缓慢漂移」。
   圆形用 `Brush.radialGradient` 画（边缘自然淡出，不需要模糊）。
3. **不要每帧分配对象**。`Brush.radialGradient` 每帧新建会有 GC 压力；
   尽量把不随帧变化的量提到 draw 之外，或用 `drawWithCache` 缓存。
4. 只有 `modifier` 与 `spec` 是入参，颜色由当前 `MaterialTheme.colorScheme` 推导 ——
   因此 4 套主题色下背景自动协调。

---

## 5. 性能与省电

应用此前无常驻动画，因此需要主动约束：

| 措施 | 说明 |
|---|---|
| **仅在 RESUMED 时运行** | 用 `LocalLifecycleOwner` + `repeatOnLifecycle(RESUMED)` 驱动；暂停/后台时停掉动画。否则锁屏或切后台仍在空耗电量 |
| 绘制预算 | ≤ 8 个径向渐变圆 + 1 个基底渐变。**不引入模糊、不引入位图、不做全屏逐像素计算** |
| 不参与布局 | 背景用 `drawBehind`/`Canvas` 绘制，不影响测量与布局 |
| 与棋盘动画共存 | 棋盘上同时可能出现连线/消除/抖动三种动画。背景必须是最轻的那个 —— 真机验收要专门看消除动画期间是否掉帧 |

**无法在编码侧测量帧率**（本机无模拟器），因此 §9 的真机验收里有一项专测流畅度。

---

## 6. 尊重系统的「减少动效」

Android 通过 `Settings.Global.ANIMATOR_DURATION_SCALE` 表达「移除动画」偏好
（无障碍设置里的「移除动画」以及部分省电模式会置 0）。

建议：读取该值，为 0 时**渲染静态版本**（即当前的渐变），不做任何运动。

这是一条低成本、高价值的无障碍处理 —— 而且比新增一个应用内开关更符合用户预期
（用户已经在系统层面表达过偏好，不该再让他进设置里关一次）。

待确认项 §13 Q3。

---

## 7. SRS 修订

| 版本 | 内容 | 性质 |
|---|---|---|
| **V1.7** | 5.1 界面清单补**启动图标**要求；NFR-1 补「启动过程无空白屏」要求 | **补做**（V1.4 遗漏，见 §2.2）—— 修订历史中会注明是补齐而非新增 |
| **V1.8** | 新增动态背景的功能要求（FR-13.9：背景可含缓慢运动的装饰元素，且不得使用承载游戏语义的强调色）；NFR-1 或 NFR-5 补「不得干扰棋盘辨识」；若采纳 §6 则补「尊重系统减少动效偏好」 | V1.5 本版本 |

---

## 8. 风险

| 编号 | 风险 | 影响 | 应对 |
|---|---|---|---|
| R-1 | **干扰玩法** —— 玩家扫视棋盘时分心 | **高** —— 直接损害核心体验 | §3 的五条硬约束（周期/透明度/数量/形状/颜色）+ 真机确认；若真机感觉干扰，优先调低透明度再考虑减少数量 |
| R-2 | 背景与真实牌混淆（形状或图案相似） | **高** | 禁止圆角方形与皮肤图案；真机在 4 套主题 × 3 套皮肤下各看一眼 |
| R-3 | 首次引入常驻动画导致功耗/发热上升 | 中 | §5 的四条措施；只在 RESUMED 运行 |
| R-4 | 与棋盘动画抢帧，消除动画期间掉帧 | 中 | 绘制预算上限；真机专测消除瞬间 |
| R-5 | 每帧重组导致性能塌方（把动画状态读在组合期） | 中 | §4 第 1 条：必须在 `Canvas` 的 draw lambda 内读 |
| R-6 | 每帧新建 `Brush` 造成 GC 抖动 | 低 | §4 第 3 条 |
| R-7 | 补做 SRS V1.7 时误把 V1.4 的要求写成「新增」 | 低 | 修订历史中明确写「补齐 V1.4 遗漏」 |

---

## 9. 验收清单

### 自动（编码侧）

| 编号 | 内容 |
|---|---|
| A-1 | `BackgroundSpec` 的约束不变量有单测：透明度 ≤ 0.08、数量 ≤ 8、周期 ≥ 20s |
| A-2 | 背景用色**不包含** `primary`/`tertiary`/`error`/`onPrimary` 等承载语义的角色（断言色值与这些角色不相等） |
| A-3 | 「系统减少动效」的判定是纯函数并有单测（scale == 0 → 静态） |
| A-4 | 全量 JVM 单测全绿（当前 194，预计 200 左右）；`assembleDebug` / `assembleRelease` 成功 |
| A-5 | `domain/` 内仍无 `android.*` 导入；release 签名密钥不变 |

### 真机（需开发者确认）

| 编号 | 内容 |
|---|---|
| V-1 | 背景确实在缓慢运动，**但第一眼不会注意到它在动**（这是本项的验收标准） |
| V-2 | 主菜单 / 关卡选择 / 设置 / 游戏界面都生效，且切换界面时不闪断 |
| V-3 | **4 套主题色 × 3 套皮肤下各看一眼**：无与牌混淆的元素、配色协调 |
| V-4 | **游戏界面玩一局**：找配对时背景不干扰；消除动画期间不掉帧 |
| V-5 | 切后台再回来：动画正常恢复；锁屏放置 5 分钟无明显发热（对比 V1.4 的观感） |
| V-6 | 系统开启「移除动画」后：背景变为静态 |
| V-7 | 若采纳 §13 Q2 的棋盘底板：牌与背景边界清晰，且与 V1.4 的观感相比不显突兀 |
| V-8 | 回归：V1.4 的 V-4 / V-6（启动无白屏、无配色跳变）与 V1.3 的 4 套主题色切换不受影响 |

---

## 10. 提交规划

```
docs:        补做 SRS V1.7（V1.4 遗漏的图标与启动体验要求）
feat(ui):    新增 AnimatedBackground 与 BackgroundSpec（含约束不变量）
feat(ui):    接入动态背景，仅在 RESUMED 时运行并尊重系统减少动效
docs:        SRS V1.8 与 V1.5 实施结果
```

`versionCode = 6`、`versionName = "1.5"` 在最后一个功能提交中一并更新。

---

## 11. 工作量

约 **1–1.5 人日**。分解：`BackgroundSpec` + `AnimatedBackground` 实现 ≈ 3h；
约束不变量测试 ≈ 1h；生命周期门控与减少动效 ≈ 2h；SRS V1.7 补做 + V1.8 ≈ 2h；
调参与文档 ≈ 2h。

**主要成本在调参**（透明度、周期、数量三者要在真机上找到「有生气但不干扰」的平衡点），
而这部分**只能靠真机反复看**，因此本项的真机验收轮次可能不止一次。

---

## 12. 与遗留项的关系

以下遗留项**不在本版本范围内**，列出以免混淆：

| 遗留项 | 状态 | 为何不并入 |
|---|---|---|
| `primary` 偏暗 | 已决定暂不调整（V1.3 计划 §12 有完整执行路径） | 与背景无关；且它要拆开被复用的颜色角色，属独立改动 |
| 开启 R8 混淆 | 待做 | 需要真机回归确认 keep 规则，与本版本无关 |
| 皮肤携带配色 | 已明确不做（V1.3 计划 §9 Q1） | 方向已定为「配色与皮肤正交」 |

---

## 13. 待确认项 —— 已确认（2026-09-12）

### Q1：视觉构想 → **A（柔和光斑缓慢漂移）**

若干个大尺寸、极低透明度的径向渐变圆，各自以不同相位缓慢漂移。不含锐利边缘、
不像任何具象物体，因此最不容易与牌混淆。

### Q2：棋盘底板 → **B（不加）**

保持现状，牌继续直接浮在背景上。

> **连带影响**：§9 的验收项 **V-7（棋盘底板）作废**。
> **需要留意的后果**：牌与背景的边界会随光斑经过而略有变化。由于光斑透明度 ≤ 0.08
> 且运动极慢，预计影响可忽略；但这一条应在真机 V-4（玩一局找配对时是否干扰）中一并留意 ——
> 若真机发现边界感不稳定，最省事的补救是**降低光斑透明度**而非回头加底板。

### Q3：系统「减少动效」偏好 → **B（不读，始终动画）**

> **连带影响**：计划的 **§6 整节作废**（不再实现 `ANIMATOR_DURATION_SCALE` 判定），
> §9 的验收项 **A-3 与 V-6 作废**。
>
> **这是一个有意识接受的取舍，如实记录**：在系统里开启了「移除动画」的用户
> （无障碍需求或省电需求）仍会看到背景运动。这是本版本唯一一处已知的无障碍退让，
> 已记入 README 的「已知取舍」表。若日后收到相关反馈，恢复该功能的成本很低
> （约十行 + 一个纯函数测试），§6 已保留实现思路。

---

## 14. 实施结果（2026-09-12）

> **注意**：本节是**修订 0** 的记录，其实现的可见性未被真机认可（「完全看不出背景在动」）。
> **当前有效的取值、结论与验收清单见 §15（修订 1）**；本节保留下来是因为它记录了
> 「约束立在配方上」这个错误的完整形态，删掉就等于把教训一起删掉。

| 项 | 结果 |
|---|---|
| 交付 | `versionCode = 6`、`versionName = "1.5"` |
| 新增 | `ui/theme/AnimatedBackground.kt`（`BackgroundSpec` / `DefaultBackgroundSpec` / `AnimatedBackground`）、`app/src/test/.../ui/theme/AnimatedBackgroundTest.kt` |
| 改动 | `MainActivity.kt`（背景使用点改走 `AnimatedBackground`）、`app/build.gradle.kts`（版本号）、SRS（V1.7 补做 + V1.8）、README |
| 未改动 | `ui/theme/Theme.kt` 的 `appBackgroundBrush()` **保留**：它仍是动态背景的静态基底，也供非前台时的静止分支使用。`domain/**`、`Palette.kt`、`Skin.kt`、`Shape.kt`、`ui/game/**` 一律未动 |
| 验证 | `:app:assembleDebug` / `:app:assembleRelease` / `:app:testDebugUnitTest` 全部 BUILD SUCCESSFUL；单测 **200** 条、失败 0（基线 194 + 新增 6） |

### 14.1 实际取值

| 参数 | §3 硬约束 | 实际取值 |
|---|---|---|
| 数量 | ≤ 8 | **5** |
| 单个光斑透明度 | ≤ 0.08 | **0.055**（约上限的 2/3，给真机调参留余量） |
| 周期 | ≥ 20 s | **30 s**（三路速度另取 ×1.31、×1.73，逐路都 ≥ 30 s） |
| 半径 | —— | **屏幕短边的 0.35 ~ 0.70** |
| 用色 | 仅中性色 | `surfaceVariant` / `outlineVariant` / `onSurfaceVariant` |

取值理由写在 `DefaultBackgroundSpec` 的 KDoc 里（含每个数字对应哪条约束），并由
`AnimatedBackgroundTest` 的 6 条断言钉住 —— 其中「透明度」一条同时断言**运行时实际调用的**
`backgroundBlobAlpha()`，而不只是断言常量。

### 14.2 与计划的差异

| 项 | 计划 | 实装 | 原因 |
|---|---|---|---|
| §4 的 `BackgroundSpec` 字段 | `blobCount` / `alpha` / `periodMillis` / `colors` | `blobCount` / `maxAlpha` / `periodMillis` / `radiusRange` | ① 颜色必须由当前 `ColorScheme` 推导（4 套主题色下都要成立），塞进常量对象里反而会诱使调用方写死色值；② `alpha` 拆成「上限 `maxAlpha`」与「每个光斑的实际取值」，是为了让「单个光斑 ≤ 0.08」这条约束在**运行时**也能被断言；③ 半径区间是 §3 之外的补充约束，写进 spec 才能被单测覆盖 |
| §5 的生命周期门控 | 「用 `repeatOnLifecycle(RESUMED)` 驱动」 | `LifecycleResumeEffect` + **把动画组合整段移出组合树** | `InfiniteTransition` 没有「暂停」API，只有在非 RESUMED 时让承载它的 composable 离开组合，才能真的取消其协程与帧请求（而不是「每帧画一个不动的位置」这种假停）。`LifecycleResumeEffect` 就是 `repeatOnLifecycle(RESUMED)` 的 composable 形式 |
| §4 第 3 条（避免每帧分配） | 「尽量把不随帧变化的量提到 draw 之外，或用 `drawWithCache` 缓存」 | 画笔缓存 + **画布平移** | 光斑的半径/颜色/透明度都不随帧变化，只有位置在变；把位置交给 `withTransform { translate(...) }`，每个光斑的 `Brush` 就能整帧复用 —— 稳态下每帧零 `Brush` 分配（`Offset`/`Color` 是 JVM 值类，本身不产生堆分配） |

**Q2（不加棋盘底板）的连带影响**：§9 的 **V-7 作废**，代码里没有任何棋盘侧改动；牌继续直接浮在背景上。

**Q3（不读系统「减少动效」）的连带影响**：**§6 整节未实施**，**A-3 与 V-6 作废**；
代码里不存在 `ANIMATOR_DURATION_SCALE` 判定，测试里也没有相关用例
（留一个永远通过的测试反而会让人以为该功能已实现）。取舍已记入 README 的「已知取舍」表。
`AnimatedBackground.kt` 里的 `StaticBackground` 分支正是日后恢复该功能所需的落点。

### 14.3 计划未覆盖、实施时做的两个决定

1. **非前台时背景退化成 V1.4 的那层静态渐变**（而不是「冻结在最后一帧的光斑」）。
   应用不在前台时人看不到它，没必要为「看起来连续」多维护一份冻结逻辑。
   副作用：再次回到前台时相位从 0 重新开始，光斑位置会跳一次 —— 透明度只有 0.055 上下、
   移动极慢，预计看不出；**列入 V-5 的真机观察项**。若要消除，需额外记录相位基准（约十行）。
2. **每帧的绘制调用固定在 6 次以内**（1 次基底 `drawRect` + 5 次光斑 `drawCircle`），
   无模糊、无位图、无逐像素运算（§5 的绘制预算）。

### 14.4 仍需真机验收的项

自动化部分（A-1、A-2、A-4、A-5）已由构建与单测覆盖；**A-3 作废**。以下仍需开发者真机确认：

| 编号 | 内容 |
|---|---|
| V-1 | 背景确实在缓慢运动，但**第一眼不会注意到它在动**（若太显眼，先降 `maxAlpha`，这是 §13 Q2 指定的首选补救） |
| V-2 | 主菜单 / 关卡选择 / 设置 / 游戏界面都生效，切换界面时不闪断 |
| V-3 | 4 套主题色 × 3 套皮肤下各看一眼：无与牌混淆的元素、配色协调 |
| V-4 | 玩一局：找配对时背景不干扰；消除动画期间不掉帧；**并留意牌与背景的边界感是否稳定**（Q2 = 不加底板，这是该决策唯一的风险点） |
| V-5 | 切后台再回来：动画正常恢复（**重点看光斑位置那一次跳变是否可察觉**）；锁屏放置 5 分钟无明显发热 |
| V-8 | 回归：V1.4 的 V-4 / V-6（启动无白屏、无配色跳变）与 V1.3 的 4 套主题色切换不受影响 |

**V-6、V-7 作废**（分别因 Q3 = B、Q2 = B）。

---

## 15. 修订 1：真机反馈「完全看不出背景在动」（2026-09-14）

§14 的实现在真机上被判定为**功能未达成**：开发者在四个界面（主菜单、关卡选择、设置、
游戏界面）都看不出背景有任何变化。以下记录排查、结论与改法。

### 15.1 排查结论：两个**互相独立**的原因，各自都足以让背景看起来完全没变

| # | 原因 | 证据 |
|---|---|---|
| **A** | **两层界面把背景整层盖住了。** `LevelSelectScreen` 与 `SettingsScreen` 用了 `Scaffold`，而 `Scaffold` 的默认 `containerColor` 是 `colorScheme.background` —— 一个**不透明**的满屏 `Surface`。这两屏因此完全没有背景（连 V1.4 的静态渐变也一直被盖着，只是没人注意过）。主菜单与游戏界面没有 `Scaffold`，不受此影响 | 源码：两处 `Scaffold` 均未传 `containerColor`；`MenuScreen` / `GameScreen` 无 `Scaffold`、无 `background` |
| **B** | **光斑在数学上就看不见。** 像素差 = 透明度 ×（底色 − 光斑色）。初版光斑取 `surfaceVariant` / `outlineVariant`，与底色 `#F8F9FC` 的差只有 29 / 57 级，乘上 0.033~0.055 的透明度后**峰值像素差只有 1~2 级（约 0.5%）**，远低于人眼在大面积色块上的察觉阈值 | 复算（4 套主题色 × 逐光斑）：`0.96 / 2.51 / 10.12 / 0.96 / 2.51` 级 |

> **B 是本版本最值得记住的一条**：§3 把约束立在**配方**上（「透明度 ≤ 0.08」），
> 而真正决定成败的是**成品**（合成后的像素差）。初版 6 条单测全绿，测的全是配方 ——
> 配方合法、成品不可见。详见 §15.4。

排查时还顺手排除了两个假设：① 应用确实装的是 V1.5 包（开发者自建安装）；
② 生命周期门控没有把动画卡在静止分支（`LifecycleResumeEffect` 在已 RESUMED 时会立即派发
`ON_RESUME`，`isResumed` 会被置真）。

### 15.2 第三处改动：不再依赖「画布变换是否作用于画笔着色器」

初版把画笔圆心固定在原点、再用 `withTransform { translate(...) }` 把画布挪到光斑位置，
好处是画笔可跨帧复用（§14.2 记为「稳态下每帧零 Brush 分配」）。

代价是它依赖一条**本机无法验证**的平台行为：画布变换是否同样作用于渐变着色器。
本机是虚拟化环境、起不了模拟器，唯一能证实它的办法是渲染出来看 —— 而这正是做不到的事。
（按 Skia 与 Android 的通行语义，变换是作用于着色器的，初版大概率没有错；
但「大概率」不足以作为一次真机返工的理由。）

因此本版改为**按光斑实际圆心创建画笔**（`Brush.radialGradient` 的 `center` 与
`drawCircle` 的 `center` 传同一个值），代价是每帧新建 4 个短命画笔，收益是这条依赖归零。
§14.2 关于「每帧零分配」的那一行随之作废，改为「每帧 ≤ 4 个小对象」。

### 15.3 视觉构想改为参照开发者的既有实现

开发者提供了参照效果：`Birthday` 项目的 `app/src/main/java/com/example/birthday/ui/components/FloatingOrbs.kt`
（三个半透明光球**缓缓上浮**并左右摆动，透明度另有一层更快的「呼吸」）。
本版据此重做形态与节奏。

**两处必须偏离参照实现的地方**（都源于两个项目的底色相反）：

| 项 | 参照实现（Birthday） | 本实现（连连看） | 原因 |
|---|---|---|---|
| 明暗关系 | 底色是饱和深紫—品红渐变（`#3D1D6E`→`#7B2FBE`→`#D81B60`），光斑用**白色**（比底色亮） | 底色是近乎白的浅色渐变（`#F8F9FC`），光斑只能用**比底色暗**的中性色 | 比近白更亮的颜色不存在。观感因此是「淡雾上浮」而非「白色光球」，这是浅色主题下的必然结果 |
| 透明度 | 峰值 0.16（白色 × 深底） | 峰值 0.10（中性色 × 浅底） | 「看得见」取决于合成后的像素差。参照实现在深底上的像素差约 20~31 级，本实现在浅底上取 12.6~20.3 级，量级相当 |

若日后想要参照实现那种「白色光球」的原味观感，前提是底色改成饱和或深色的渐变 ——
那会与 V1.3 的「4 套浅色主题色、无深色模式」决策冲突，不是本版本能顺手做的事。

### 15.4 修订后的取值

| 参数 | 修订前 | 修订后 | 依据 |
|---|---|---|---|
| 形态 | 5 个大光斑沿椭圆轨迹漂移 | **4 个光斑从屏幕下缘之外升到上缘之外**，横向摆动 2 圈 | 参照实现；上浮的起止点都在屏幕之外，因此循环处天然无缝（修订前靠 sin/cos 保证连续） |
| 数量 | 5 | **4** | 参照实现是 3 个 |
| 峰值透明度 | 0.055 | **0.10**（上限 0.12） | 由像素差反推，见下 |
| 用色 | `surfaceVariant` / `outlineVariant` / `onSurfaceVariant` | **`onSurfaceVariant` / `onSurface`** | 两者都是中性角色（不是语义色），且与底色的差为 184~226 级，乘上透明度后落在可见区间 |
| 上浮周期 | 30 s（三路 ×1.31 / ×1.73） | **20 s（四路 ×1.0 / ×1.25 / ×0.85 / ×1.45 = 17~29 s）** | 参照实现是 10/12/14 s，本项目放慢但仍明显在动；下限从 20 s 放宽到 15 s 并逐路断言 |
| 半径（相对短边） | 0.35 ~ 0.70 | **0.22 ~ 0.34** | 参照实现是 0.24~0.36。刻意**不做成覆盖半屏的巨大光斑**：光斑越大，同一时刻被压暗的面积越大，棋盘上「牌与背景的边界」被削弱的范围也越大（Q2 = B 没有底板，这是该决策唯一的风险点） |
| 呼吸 | 无 | **透明度在峰值的 35%~100% 之间以 3 倍速摆动** | 参照实现；下限刻意不是 0（参照实现会降到 0，在浅色底上会变成「一块雾忽然出现又消失」） |

**§3 的约束随之修正**：`透明度 ≤ 0.08、周期 ≥ 20 s` 改为
**`每个光斑的峰值像素差 ∈ [11, 28] 级（0~255）、周期 ≥ 15 s、数量 3~8`**。
即把约束从「配方」改到「成品指标」上，配方数字只是它的副产品。

### 15.5 测试相应重写：从「测配方」改为「测成品」

`AnimatedBackgroundTest` 由 6 条改写为 **8 条**，新增的核心一条是：

```
每个光斑在其呼吸峰值处都看得见
  → 逐主题色 × 逐光斑 × 渐变的亮端与暗端，
    断言 backgroundOrbComposite(base, orb, alpha) 与 base 的最大单通道差 ∈ [11, 28]
```

它调用的是**运行时真正使用的**那两个函数（`backgroundOrbPeakAlpha` / `backgroundOrbColors`），
而不是另抄一份期望值。**这条断言在初版实现上会立刻失败**（实测 `0.96 / 2.51 / 10.12 / 0.96 / 2.51`
级，4 个光斑低于门槛）—— 这正是它存在的意义：把「配方合法但成品看不见」这类事故变成红灯。

另新增「呼吸不会让光斑完全消失」一条；「透明度不超上限」一条保留，但降级为配方检查，
并在注释里写明它不是可见性的保证。

### 15.6 仍需真机确认（取代 §14.4）

| 编号 | 内容 |
|---|---|
| V-1 | **主菜单**上能看出光斑在缓慢上浮，且不至于抢注意力（太浓 → 降 `maxAlpha`；太淡 → 升，上限 0.12） |
| V-2 | **关卡选择与设置**两屏同样能看到背景（本次修订的直接目标之一） |
| V-3 | 4 套主题色 × 3 套皮肤下各看一眼：配色协调、无与牌混淆的元素 |
| V-4 | 玩一局：找配对时不干扰；消除动画期间不掉帧；**并重点看牌与背景的边界感**（Q2 = B 无底板；若边界发糊，补救顺序是：先降 `maxAlpha` → 仍不行则给棋盘加底板（撤销 Q2）或给未选中的牌补极浅描边） |
| V-5 | 切后台再回来：光斑会**同时从屏幕下方重新升起**（修订前的「位置跳变」已变成这种更自然的重启）；锁屏放置 5 分钟无明显发热 |
| V-8 | 回归：V1.4 的启动无白屏、无配色跳变，与 V1.3 的 4 套主题色切换不受影响 |

